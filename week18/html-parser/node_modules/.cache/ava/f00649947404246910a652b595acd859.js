"use strict";

var _parser = require("../src/parser.js");

var _ava = _interopRequireDefault(require("ava"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { "default": obj }; }

function _createForOfIteratorHelper(o, allowArrayLike) { var it; if (typeof Symbol === "undefined" || o[Symbol.iterator] == null) { if (Array.isArray(o) || (it = _unsupportedIterableToArray(o)) || allowArrayLike && o && typeof o.length === "number") { if (it) o = it; var i = 0; var F = function F() {}; return { s: F, n: function n() { if (i >= o.length) return { done: true }; return { done: false, value: o[i++] }; }, e: function e(_e) { throw _e; }, f: F }; } throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); } var normalCompletion = true, didErr = false, err; return { s: function s() { it = o[Symbol.iterator](); }, n: function n() { var step = it.next(); normalCompletion = step.done; return step; }, e: function e(_e2) { didErr = true; err = _e2; }, f: function f() { try { if (!normalCompletion && it["return"] != null) it["return"](); } finally { if (didErr) throw err; } } }; }

function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }

function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) { arr2[i] = arr[i]; } return arr2; }

(0, _ava["default"])("parse a single element", function (t) {
  var doc = (0, _parser.parseHTML)("<div></div>");
  var div = doc.children[0];

  if (div.tagName == "div") {
    t.pass();
  }

  if (div.children.length === 0) {
    t.pass();
  }

  if (div.type === "element") {
    t.pass();
  }

  if (div.attributes.length === 2) {
    t.pass();
  }
});
(0, _ava["default"])("parse a single element with text content", function (t) {
  var doc = (0, _parser.parseHTML)("<div>hello</div>");
  var text = doc.children[0].children[0];

  if (text.content === "hello") {
    t.pass();
  }

  if (text.type === "text") {
    t.pass();
  }
});
(0, _ava["default"])("tag mismatch", function (t) {
  try {
    var doc = (0, _parser.parseHTML)("<div></p>");
  } catch (e) {
    if (e.message === "Tag start end doesn't match!") {
      t.pass();
    }
  }
});
(0, _ava["default"])("test with <", function (t) {
  var doc = (0, _parser.parseHTML)("<div>a < b</div>");
  var text = doc.children[0].children[0];

  if (text.content === "a < b") {
    t.pass();
  }

  if (text.type === "text") {
    t.pass();
  }
});
(0, _ava["default"])("with property", function (t) {
  var doc = (0, _parser.parseHTML)("<div id=a class='cls' data=\"abc\" ></div>");
  var div = doc.children[0];
  var count = 0;

  var _iterator = _createForOfIteratorHelper(div.attributes),
      _step;

  try {
    for (_iterator.s(); !(_step = _iterator.n()).done;) {
      var attr = _step.value;

      if (attr.name === "id") {
        if (attr.value === "a") {
          count++;
          t.pass();
        }
      }

      if (attr.name === "class") {
        if (attr.value === "cls") {
          count++;
          t.pass();
        }
      }

      if (attr.name === "data") {
        if (attr.value === "abc") {
          count++;
          t.pass();
        }
      }
    }
  } catch (err) {
    _iterator.e(err);
  } finally {
    _iterator.f();
  }
});
(0, _ava["default"])("with double quoted property", function (t) {
  var doc = (0, _parser.parseHTML)("<div id=a class='cls' data=\"abc\"></div>");
  var div = doc.children[0];
  var count = 0;

  var _iterator2 = _createForOfIteratorHelper(div.attributes),
      _step2;

  try {
    for (_iterator2.s(); !(_step2 = _iterator2.n()).done;) {
      var attr = _step2.value;

      if (attr.name === "id") {
        if (attr.value === "a") {
          count++;
          t.pass();
        }
      }

      if (attr.name === "class") {
        if (attr.value === "cls") {
          count++;
          t.pass();
        }
      }

      if (attr.name === "data") {
        if (attr.value === "abc") {
          count++;
          t.pass();
        }
      }
    }
  } catch (err) {
    _iterator2.e(err);
  } finally {
    _iterator2.f();
  }
});
(0, _ava["default"])("with self close", function (t) {
  var doc = (0, _parser.parseHTML)("<div id=a class='cls' data=\"abc\"/>");
  var div = doc.children[0];
  var count = 0;

  var _iterator3 = _createForOfIteratorHelper(div.attributes),
      _step3;

  try {
    for (_iterator3.s(); !(_step3 = _iterator3.n()).done;) {
      var attr = _step3.value;

      if (attr.name === "id") {
        if (attr.value === "a") {
          count++;
          t.pass();
        }
      }

      if (attr.name === "class") {
        if (attr.value === "cls") {
          count++;
          t.pass();
        }
      }

      if (attr.name === "data") {
        if (attr.value === "abc") {
          count++;
          t.pass();
        }
      }
    }
  } catch (err) {
    _iterator3.e(err);
  } finally {
    _iterator3.f();
  }
});
(0, _ava["default"])("script", function (t) {
  // let content = `
  // <div>abcd</div>
  // <span>x/span>
  // /script>
  // <script
  // <
  // </
  // </sc
  // </scr
  // </scri
  // </scrip
  // </script`;
  var doc = (0, _parser.parseHTML)("<script>\n  <div>abcd</div>\n  <span>x/span>\n  /script>\n  <script\n  <\n  </\n  </sc\n  </scr\n  </scri\n  </scrip\n  </script\n  </script>");
  var text = doc.children[0].children[0];
  t.pass(); // if (text.content === content) {
  //   t.pass();
  // }
  // if (text.type === "text") {
  //   t.pass();
  // }
});
(0, _ava["default"])("attribute with no value", function (t) {
  var doc = (0, _parser.parseHTML)("<div class/>");
  t.pass();
});
(0, _ava["default"])("attribute with no value 2", function (t) {
  var doc = (0, _parser.parseHTML)("<div class id/>");
  t.pass();
});
(0, _ava["default"])("self", function (t) {
  var doc = (0, _parser.parseHTML)("<div/>");
  t.pass();
});
(0, _ava["default"])("self", function (t) {
  var doc = (0, _parser.parseHTML)("<div class='class'/>");
  t.pass();
});
//# sourceMappingURL=/Users/caihuiting/Desktop/前端学习资料/Github/week18/html-parser/node_modules/.cache/ava/f00649947404246910a652b595acd859.js.map